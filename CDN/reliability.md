What "reliability" mean
    Survive failures without going dark
    reroute traffic automatically
    show something useful (like a status page) while you fix it.

Edge fallbacks (when stuff breaks)
    serve a cached/static fallback page at the edge
    show status + links that still work